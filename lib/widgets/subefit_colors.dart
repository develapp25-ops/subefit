import 'package:flutter/material.dart';

class SubefitColors {
  // --- Paleta Principal (Rojo, Blanco, Negro) ---
  static const Color primaryRed = Color(0xFFE50914); // Rojo Netflix-style
  static const Color appBlack = Color(0xFF121212); // Negro profundo para fondos
  static const Color appWhite = Colors.white; // Blanco para temas claros
  static const Color lightGrey =
      Color(0xFFF5F5F5); // Gris claro para superficies en tema claro
  static const Color darkGrey =
      Color(0xFF212121); // Gris oscuro para tarjetas (casi negro)
  static const Color darkBg = Color(0xFF121212); // Alias para el fondo

  // Colores de Texto
  static const Color textWhite = Colors.white;
  static const Color textBlack = Colors.black;

  // Colores de Estado
  static const Color dangerRed = Color(0xFFD32F2F);
  static const Color secondaryOrange = Color(0xFFFFA000);
}
